# Google Places — powers the live audit.
# Without this the site falls back to deterministic mock data (see lib/audit.ts).
# Enable "Places API (New)" and restrict the key to your domain.
GOOGLE_PLACES_API_KEY=
